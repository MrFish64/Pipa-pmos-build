name: Build pipa images
on:
  push:
  workflow_dispatch:

jobs:
  build-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        
      - name: Preparation
        run: mkdir out
        
      - name: Install pmbootstrap
        run: |
          # Hex-шифр для: https://gitlab.postmarketos.org
          PMB_URL_HEX="68747470733a2f2f6769746c61622e706f73746d61726b65746f732e6f72672f706f73746d61726b65744f532f706d626f6f7473747261702e676974"
          git clone --depth=1 $(echo "$PMB_URL_HEX" | xxd -r -p) pmb-git
          mkdir -p ~/.local/bin
          ln -s "$GITHUB_WORKSPACE/pmb-git/pmbootstrap.py" ~/.local/bin/pmbootstrap
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Setup Environment and Fix Dependencies
        run: |
          mkdir -p /home/runner/.config
          
          # РАСШИФРОВКА ЗЕРКАЛ (Hex)
          ALP_HEX="687474703a2f2f646c2d63646e2e616c70696e656c696e75782e6f72672f616c70696e652f"
          PMO_HEX="687474703a2f2f6d6972726f722e706f73746d61726b65746f732e6f72672f706f73746d61726b65746f732f"
          
          ALPINE_URL=$(echo "$ALP_HEX" | xxd -r -p)
          PMOS_URL=$(echo "$PMO_HEX" | xxd -r -p)
          
          # КОНФИГ V3: Явный запрет qemu и systemd
          cat <<EOF > /home/runner/.config/pmbootstrap.cfg
          [pmb]
          config_version = 3
          aports = /home/runner/.local/var/pmbootstrap/cache_git/pmaports
          work = /home/runner/.local/var/pmbootstrap
          jobs = $(nproc)
          user = runner
          native_arch = x86_64
          [channels]
          channel = edge
          is_default_channel = False
          [device]
          device = pipa
          vendor = xiaomi
          architecture = aarch64
          [ui]
          ui = console
          systemd = never
          [install]
          boot_size = 256
          extra_space = 2048
          password = 147147
          [mirrors]
          alpine = $ALPINE_URL
          postmarketos = $PMOS_URL
          EOF
          
          mkdir -p /home/runner/.local/var/pmbootstrap/cache_git
          mkdir -p /home/runner/.local/var/pmbootstrap/chroot_native
          echo "8" > /home/runner/.local/var/pmbootstrap/version
          
          # Клон pmaports (Hex: https://gitlab.postmarketos.org)
          git clone --depth=1 $(echo "68747470733a2f2f6769746c61622e706f73746d61726b65746f732e6f72672f706f73746d61726b65744f532f706d61706f7274732e676974" | xxd -r -p) /home/runner/.local/var/pmbootstrap/cache_git/pmaports
          
          cd /home/runner/.local/var/pmbootstrap/cache_git/pmaports
          # Клон pipa (Hex: https://github.com)
          git remote add pipa $(echo "68747470733a2f2f6769746875622e636f6d2f706970612d70726f6a6563742f706d61706f7274732d706970612e676974" | xxd -r -p)
          git fetch pipa master && git reset --hard pipa/master
          
          # ФИКС 1: УДАЛЯЕМ ПАПКУ nofde (чтобы не было Pkgname != Folder)
          rm -rf main/postmarketos-base-nofde
          
          # ФИКС 2: ЗАМЕНЯЕМ УПОМИНАНИЯ nofde НА base
          find . -type f \( -name "APKBUILD" -o -name "deviceinfo" \) -exec sed -i "s/postmarketos-base-nofde/postmarketos-base/g" {} +
          
          # ФИКС 3: УБИРАЕМ ДУБЛИ (exit code 5)
          sed -i 's/doas-sudo-shim,//g;s/font-twemoji,//g' device/*/device-xiaomi-pipa/deviceinfo || true
          
          pmbootstrap -c /home/runner/.config/pmbootstrap.cfg checksum device-xiaomi-pipa
          pmbootstrap -c /home/runner/.config/pmbootstrap.cfg index

      - name: Build Plasma
        run: |
           sed -i 's/^ui =.*/ui = plasma-mobile/' /home/runner/.config/pmbootstrap.cfg
           pmbootstrap -c /home/runner/.config/pmbootstrap.cfg -y zap
           # ФИКС 4: ЯВНОЕ УКАЗАНИЕ ДЕВАЙСА (против qemu)
           pmbootstrap -c /home/runner/.config/pmbootstrap.cfg install -d pipa --no-fde --add gparted,parted
           pmbootstrap -c /home/runner/.config/pmbootstrap.cfg export
           cp /tmp/postmarketOS-export/boot.img out/boot-pipa-plasma.img
           cp /tmp/postmarketOS-export/xiaomi-pipa.img out/rootfs-pipa-plasma.img

      - name: Build GNOME
        run: |
           sed -i 's/^ui =.*/ui = gnome/' /home/runner/.config/pmbootstrap.cfg
           pmbootstrap -c /home/runner/.config/pmbootstrap.cfg -y zap
           # ФИКС 5: ЯВНОЕ УКАЗАНИЕ ДЕВАЙСА (против qemu)
           pmbootstrap -c /home/runner/.config/pmbootstrap.cfg install -d pipa --no-fde --add gparted,parted,nano
           pmbootstrap -c /home/runner/.config/pmbootstrap.cfg export
           cp /tmp/postmarketOS-export/boot.img out/boot-pipa-gnome.img
           cp /tmp/postmarketOS-export/xiaomi-pipa.img out/rootfs-pipa-gnome.img

      - name: Upload
        uses: actions/upload-artifact@v4
        with:
           name: xiaomi-pipa-builds
           path: out/*
           retention-days: 7
